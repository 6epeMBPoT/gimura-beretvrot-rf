server {
	listen 443 ssl;
	server_name гимура.беретврот.рф;
	ssl_certificate /usr/src/app/certificate.pem;
	ssl_certificate_key /usr/src/app/privatekey.pem;

	location / {
		proxy_set_header Host $host;
		proxy_pass http://127.0.0.1:8000;
		proxy_redirect off;
      		break;
    	}
}
